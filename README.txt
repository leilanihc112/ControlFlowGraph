Assumptions:
-when running s.sh, add command line argument including the name of the input txt file (ex. ./s.sh test1.txt)
-input text file does not have any errors (all conditional and unconditional jumps are defined, etc.)
-no trailing white space for labels
-program is run in Python 3